/**
 * Check if a figure card is blocked
 *
 * @param {object} figureCard - The figure card to check.
 * @returns {boolean} True if the figure card is blocked, otherwise false.
 */
export const isFigureCardBlocked = (figureCard) => figureCard.isBlocked;
